[project]
name = "s11-trace-then-gate"
version = "1.0.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "arize-phoenix==12.33.1",
    "arize-phoenix-evals==2.8.0",
    "azure-identity>=1.23",
    "httpx>=0.28",
    "opentelemetry-sdk>=1.30",
    "opentelemetry-exporter-otlp-proto-http>=1.30",
    "fastapi==0.115.6",
    "starlette==0.41.3",
]

[dependency-groups]
dev = ["pytest>=8"]
